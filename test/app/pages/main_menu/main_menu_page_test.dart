import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('MainMenuPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MainMenuPage(title: 'MainMenu')));
    //  final titleFinder = find.text('MainMenu');
    //  expect(titleFinder, findsOneWidget);
  });
}
