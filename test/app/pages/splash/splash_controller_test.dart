import 'package:flutter_test/flutter_test.dart';

import 'package:raro_test/app/pages/splash/splash_controller.dart';

void main() {
  // SplashController splash;
  //
  setUp(() {
    //     splash = null.to.get<SplashController>();
  });

  group('SplashController Test', () {
    //   test("First Test", () {
    //     expect(splash, isInstanceOf<SplashController>());
    //   });

    //   test("Set Value", () {
    //     expect(splash.value, equals(0));
    //     splash.increment();
    //     expect(splash.value, equals(1));
    //   });
  });
}
