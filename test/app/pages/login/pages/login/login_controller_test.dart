import 'package:flutter_test/flutter_test.dart';

import 'package:raro_test/app/pages/login/pages/login/login_controller.dart';

void main() {
  // LoginController login;
  //
  setUp(() {
    //     login = null.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
