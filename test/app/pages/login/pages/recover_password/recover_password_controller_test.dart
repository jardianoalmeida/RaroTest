import 'package:flutter_test/flutter_test.dart';

import 'package:raro_test/app/pages/login/pages/recover_password/recover_password_controller.dart';

void main() {
  // RecoverPasswordController recoverpassword;
  //
  setUp(() {
    //     recoverpassword = null.to.get<RecoverPasswordController>();
  });

  group('RecoverPasswordController Test', () {
    //   test("First Test", () {
    //     expect(recoverpassword, isInstanceOf<RecoverPasswordController>());
    //   });

    //   test("Set Value", () {
    //     expect(recoverpassword.value, equals(0));
    //     recoverpassword.increment();
    //     expect(recoverpassword.value, equals(1));
    //   });
  });
}
